/**
 *  Just assemble Affirm libraries into one
 *
 */
(function () {
    module.exports = {
        basket: require('./basket/affirmBasket.ds'),
        data: require('./data/affirmData.ds'),
        order: require('./order/affirmOrder.ds'),
        utils: require('./utils/affirmUtils.ds')
    };
}());
